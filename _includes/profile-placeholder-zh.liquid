在这里写下你的个人简介，向世界介绍自己。你可以链接到喜欢的网站，也可以放一张照片；只需将图片命名为 `prof_pic.jpg` 并放入 `img/` 文件夹。

把地址、邮箱或其他信息放在照片下方。你也可以在 `_pages/about.md` 的 YAML 头部修改 `profile` 属性来关闭这些元素。编辑 `_bibliography/papers.bib`
后，Jekyll 会自动生成[论文页面](/publications/)。

还可以链接你的社交媒体。这个主题支持 Font Awesome 与 Academicons 图标。
