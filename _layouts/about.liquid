---
layout: default
---
<div class="post">
  <header class="post-header">
    <h1 class="post-title">
      {% if page.lang == 'zh' %}
        <span class="font-weight-bold">樊宇琛</span>
      {% elsif site.title == 'blank' %}
        <span class="font-weight-bold">{{ site.first_name }}</span> {{ site.middle_name }}
        {{ site.last_name }}
      {% else %}
        {{ site.title }}
      {% endif %}
    </h1>
    <p class="desc">{{ page.subtitle }}</p>
  </header>

  <article>
    {% if page.profile %}
      <div class="profile float-{% if page.profile.align == 'left' %}left{% else %}right{% endif %}">
        {% if page.profile.image %}
          {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
          {% if page.profile.image_circular %}
            {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
          {% else %}
            {% assign profile_image_class = 'img-fluid z-depth-1 rounded' %}
          {% endif %}
          {% capture sizes %}(min-width: {{ site.max_width }}) {{ site.max_width | minus: 30 | times: 0.3 }}px, (min-width: 576px) 30vw, 95vw{% endcapture %}
          {% if page.profile.image_srcset_webp %}
            {% assign profile_image_small = page.profile.image_srcset_webp.small | prepend: 'assets/img/' %}
            {% assign profile_image_large = page.profile.image_srcset_webp.large | prepend: 'assets/img/' %}
            <figure>
              <picture>
                <source
                  srcset="{{ profile_image_small | relative_url | bust_file_cache }} 480w, {{ profile_image_large | relative_url | bust_file_cache }} 800w"
                  sizes="{{ sizes | strip }}"
                  type="image/webp"
                >
                <img
                  src="{{ profile_image_path | relative_url | bust_file_cache }}"
                  class="{{ profile_image_class }}"
                  width="{{ page.profile.image_width | default: 800 }}"
                  height="{{ page.profile.image_height | default: 1067 }}"
                  alt="{{ page.profile.image_alt | default: page.profile.image }}"
                  loading="eager"
                  decoding="async"
                  fetchpriority="high"
                >
              </picture>
            </figure>
          {% else %}
            {%
              include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=page.profile.image
              cache_bust=true
            %}
          {% endif %}
        {% endif %}
        {% if page.profile.more_info %}
          <div class="more-info">{{ page.profile.more_info }}</div>
        {% endif %}
      </div>
    {% endif %}

    <div class="clearfix">{{ content }}</div>

    {% if page.announcements and page.announcements.enabled %}
      <h2>
        <a href="{{ page.news_path | default: '/news/' | relative_url }}" style="color: inherit">
          {{- page.labels.news | default: 'news' -}}
        </a>
      </h2>
      {% include activity-feed.liquid %}
    {% endif %}

    {% if page.social %}
      <div class="social">
        <div class="contact-icons">{% social_links %}</div>
        <div class="contact-note">{{ page.contact_note | default: site.contact_note }}</div>
      </div>

      <dialog
        id="wechat-qr-dialog"
        class="wechat-qr-dialog"
        aria-labelledby="wechat-qr-heading"
        aria-describedby="wechat-qr-description"
      >
        <form method="dialog">
          <button
            class="wechat-qr-dialog__close"
            value="close"
            aria-label="{% if page.lang == 'en' %}Close WeChat QR code{% else %}关闭微信二维码{% endif %}"
          >
            <span aria-hidden="true">&times;</span>
          </button>
        </form>
        <div class="wechat-qr-dialog__body">
          <h2 id="wechat-qr-heading">
            {% if page.lang == 'en' %}WeChat{% else %}微信{% endif %}
          </h2>
          <img
            src="{{ '/assets/img/social/wechat-qr.png' | relative_url | bust_file_cache }}"
            width="660"
            height="660"
            loading="lazy"
            decoding="async"
            alt="{% if page.lang == 'en' %}Yuchen Fan's WeChat QR code{% else %}樊宇琛的微信二维码{% endif %}"
          >
          <p id="wechat-qr-description">
            {% if page.lang == 'en' %}Scan to add me on WeChat.{% else %}扫码添加我为好友。{% endif %}
          </p>
        </div>
      </dialog>
    {% endif %}

    {% if site.newsletter and site.newsletter.enabled and site.footer_fixed %}
      {% if site.plugins contains 'al_newsletter' %}
        {% include plugins/al_newsletter_form_center_margin.liquid %}
      {% else %}
        {% include newsletter.liquid center=true %}
      {% endif %}
    {% endif %}
  </article>
</div>
